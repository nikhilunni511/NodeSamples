var Struct = require('../index.js').Struct;
 
var Person = Struct()
             .chars('firstName',12,'base64')
             .chars('lastName',12,'utf-8')
             .array('items',3,'chars',10)
             .word16Sle('balance'),
    Persons = Struct()
              .word8('presentCount')
              .array('list',2,Person);
    
Persons.allocate();

                         
var proxy = Persons.fields;

proxy.list[0].firstName = 'Иван';
proxy.list[0].lastName = 'Иванов';

console.log(proxy.list[0].firstName,proxy.list[0].lastName);
